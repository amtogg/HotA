package models.herbaceous;

import models.abstracts.Animal;

public class Mous extends Animal {
    protected Mous(Double weight, Integer maxCountOfField, Integer speed, Double kgFullEating) {
        super(weight, maxCountOfField, speed, kgFullEating);
    }
}
